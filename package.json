{
  "name": "glasscart",
  "private": true,
  "version": "0.0.0",
  "description": "GlassCart — PERN + TypeScript (vanilla storefront preserved until Phase 9)",
  "workspaces": [
    "client"
  ],
  "scripts": {
    "dev": "npm run dev -w client",
    "dev:client": "npm run dev -w client",
    "build": "npm run build -w client",
    "build:client": "npm run build -w client"
  },
  "engines": {
    "node": ">=20"
  }
}
